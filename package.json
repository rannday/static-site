{
  "name": "static-site",
  "version": "0.0.1",
  "description": "Cloudflare static site",
  "homepage": "https://rannday.github.io/static-site/",
  "bugs": {
    "url": "https://github.com/rannday/static-site/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rannday/static-site.git"
  },
  "license": "Unlicense",
  "author": "Rann Day",
  "type": "module",
  "scripts": {
    "clean": "del-cli public --force",
    "css": "esbuild src/css/highlight-light.css src/css/highlight-dark.css --bundle --outdir=public/css --minify",
    "css:watch": "esbuild src/css/highlight-light.css src/css/highlight-dark.css --bundle --outdir=public/css --watch",
    "js": "esbuild src/js/construction.js src/js/links.js src/js/highlight.js --bundle --outdir=public/js --minify",
    "js:watch": "esbuild src/js/construction.js src/js/links.js src/js/highlight.js --bundle --outdir=public/js --minify --watch",
    "serve": "eleventy --serve",
    "test": "npx serve public -l 8080",
    "dev": "npm-run-all --parallel css:watch js:watch serve",
    "build": "npm run clean && npm run css && npm run js && cross-env ELEVENTY_ENV=production eleventy",
    "deploy": "npm run build && wrangler pages deploy public --project-name=rannday"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "cross-env": "^7.0.3",
    "del-cli": "^6.0.0",
    "esbuild": "^0.25.8",
    "html-minifier-next": "^1.1.4",
    "npm-run-all": "^4.1.5",
    "the-new-css-reset": "^1.11.3",
    "wrangler": "^4.25.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "lucide": "^0.525.0"
  }
}
